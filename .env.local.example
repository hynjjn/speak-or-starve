AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=koreacentral

# Set to 1 to surface a SKIP button in the SPEAK panel that bypasses the mic
# and auto-passes the mission. Leave unset / =0 in production.
NEXT_PUBLIC_DEBUG_MODE=0
